create table login
(id_n int not null,
id varchar(20) not null unique,
password int not null,
primary key (id_n))

create table friend_list
(id_n int,
id varchar(20),
f_id_n int,
f_id varchar(20),
primary key (id_n, f_id))

create table detail_imf(
id_n int,
id varchar(20) not null,
password int not null,
image blob,
stm_messge varchar(30), 
primary key(id_n))

select *
from login_test;

insert into login_test
values('client1', 1234);


select login.id
from login, friend_list
where login.id = 'dbtmddus112';

insert into friend_list
values('dbtmddus112', 'db_f1')

alter table login add(identification_number int unique);


alter table login 
drop column identification_number;
//삭제

// 친구 목록 호출	-> 오류
select friend_list.f_id 
from login,friend_list 
where login.id_n = 1;


//
select friend_list.f_id 
from login,friend_list 
where login.id_n = friend_list.id_n and login.id_n = 1;


insert into detail_imf(stm_message)
values('abcd');