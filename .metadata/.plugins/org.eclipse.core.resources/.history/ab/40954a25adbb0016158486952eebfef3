
import java.awt.BorderLayout;
import java.awt.ComponentOrientation;
import java.awt.GridLayout;
import java.awt.Panel;
import java.awt.event.ActionEvent;
import java.awt.event.ActionListener;
import java.util.Vector;

import javax.swing.JButton;
import javax.swing.JFrame;
import javax.swing.JPanel;
import javax.swing.JScrollBar;
import javax.swing.JScrollPane;
import javax.swing.JTextField;

public class swing_test extends Panel{

	private JTextField ID_textfield;
	private Panel[] panel = new Panel[100];
	
	static int f_list_size;
	static String[] f_list;

	static Vector<JPanel> vector_jpanel = new Vector<JPanel>(0); 
	
	public swing_test(){
		super();
		setComponentOrientation(ComponentOrientation.LEFT_TO_RIGHT);
		setLayout(new GridLayout(100, 1, 3, 2));
		setVisible(true);
		setSize(460, 226);

		//set_friend_db();

		JButton b0= new JButton("dd");
		JButton b1= new JButton("dd");
		JButton b2= new JButton("dd");
		add(b0);
		add(b1);
		add(b2);
		
		/*JPanel panel0 = new JPanel();
		add(panel0);
		vector_jpanel.addElement(panel0);	
		JButton b0= new JButton("dd");
		panel0.add(b0);
		*/
		/*for (int i=0; i<10; i++){
			make_one_f_room();
		}*/
	}//end creator

	public void make_one_f_room(){
		JPanel panel = new JPanel();
		add(panel);
		vector_jpanel.addElement(panel);	
		JButton b = new JButton("dd");
		panel.add(b);
	}
	
	ActionListener action = new ActionListener() {

		public void actionPerformed(ActionEvent e) {

			Object obj = e.getSource();
			/*if (obj==login_button){

			}
			else if (obj==signin_button){	
			}*/
		}
	};
	
	public static void main(String[] args) {

		//swing_test p1 = new swing_test();
		
		JFrame frame = new JFrame("dd");
		frame.setVisible(true);
		frame.setDefaultCloseOperation(JFrame.EXIT_ON_CLOSE);
		frame.setSize(460, 226);
		frame.getContentPane().setLayout(null);
		
		
		JScrollPane spanel = new JScrollPane();
		spanel.setVisible(true);
		spanel.setSize(460, 226);
		frame.getContentPane().add(spanel);
		
		JButton b0= new JButton("dd");
		b0.setBounds(112, 128, 116, 24);	//안하면 안나옴 (아마 크기 0인듯)
		
		JButton b1= new JButton("dd");
		JButton b2= new JButton("dd");
		spanel.add(b0, BorderLayout.NORTH);
		spanel.add(b1, BorderLayout.NORTH);
		spanel.add(b2, BorderLayout.NORTH);
		//panel.setComponentOrientation(ComponentOrientation.LEFT_TO_RIGHT);
		
	}
}//end

