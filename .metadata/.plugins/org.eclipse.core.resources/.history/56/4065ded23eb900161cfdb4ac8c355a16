package client_swing;

import java.awt.BorderLayout;
import java.awt.EventQueue;

import javax.swing.JFrame;
import javax.swing.JPanel;
import javax.swing.border.EmptyBorder;
import javax.swing.JMenuBar;
import javax.swing.JMenu;
import javax.swing.JButton;
import java.awt.Panel;
import javax.swing.event.ChangeListener;

import client_obj.friend_panel;

import javax.swing.event.ChangeEvent;
import java.awt.event.ActionListener;
import java.awt.event.ActionEvent;

public class main_frame extends JFrame {


	private friend_panel p_friend;
	
	private JPanel contentPane;

	private JMenuBar menuBar;
	private JButton b_friend;
	private JButton b_chat;
	private JButton b_configuration;
	
	public main_frame() {
		
		p_friend = new friend_panel(_connected_socket, _in, _out)
		getContentPane().setLayout(null);
		setVisible(true);

		setDefaultCloseOperation(JFrame.EXIT_ON_CLOSE);
		setBounds(100, 100, 395, 650);

		menuBar = new JMenuBar();
		setJMenuBar(menuBar);

		b_friend = new JButton("\uCE5C\uAD6C\uBAA9\uB85D");
		b_friend.addActionListener(new ActionListener() {
			public void actionPerformed(ActionEvent arg0) {
			}
		});
		b_friend.addChangeListener(new ChangeListener() {
			public void stateChanged(ChangeEvent arg0) {
			}
		});
		menuBar.add(b_friend);

		b_chat = new JButton("\uB300\uD654\uBAA9\uB85D");
		b_chat.addActionListener(new ActionListener() {
			public void actionPerformed(ActionEvent e) {
			}
		});
		menuBar.add(b_chat);

		b_configuration = new JButton("\uC124\uC815");
		menuBar.add(b_configuration);

		JButton b_add_friend = new JButton("\uCE5C\uAD6C\uCD94\uAC00");
		b_add_friend.addActionListener(new ActionListener() {
			public void actionPerformed(ActionEvent e) {
			}
		});
		menuBar.add(b_add_friend);

		contentPane = new JPanel();
		contentPane.setBorder(new EmptyBorder(5, 5, 5, 5));
		contentPane.setLayout(new BorderLayout(0, 0));
		setContentPane(contentPane);

		Panel p_main = new Panel();
		contentPane.add(p_main, BorderLayout.CENTER);
	}

	ActionListener action = new ActionListener() {

		public void actionPerformed(ActionEvent e) {

			Object obj = e.getSource();

			if (obj==b_friend){
				
				setContentPane();

			}
			else if (obj==signin_button){

			}
		}
	};

}

